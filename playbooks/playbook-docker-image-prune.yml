- hosts: docker
  become: true
  tasks:

  - name: Install pip
    apt:
      update_cache: yes
      name:
        - python3-pipx

  - name: Install pexpect
    command: pipx install pexpect
      
  - name: Prune Unused Docker Images 
    ansible.builtin.expect:
      command: docker image prune
      responses:
        'Confirmation': 'y'
      
